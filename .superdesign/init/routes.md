# Routes

This is a single-page Wails React frontend. There is no client-side router.

- `/` — `desktop-app/frontend/src/main.tsx`, App shell/workbench
- Wails bindings are exposed through `window.go.main.App`.
