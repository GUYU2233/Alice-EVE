# EVE 信息助理参考项目分析与架构建议

> 分析对象：`参考项目/` 中已拉取的 EVE 相关项目。本文记录项目定位、技术取舍、可复用设计和目标应用架构建议。当前技术路线已调整为 **Go + Wails**。
>
> **2026-09-16 架构校正：** 本文早期章节仍保留了调研阶段的 Node/Fastify、SSE 和“服务器绝不保存 EVE Token”等历史方案，便于追溯决策，但它们不再是当前实施依据。当前实施基线以本文第 10 节《数据平台架构与实施总计划》和 `当前工作记录.md` 为准；任何与前文冲突的内容，以第 10 节为准。

## 1. 参考项目总览

| 项目 | 技术栈/形态 | 主要价值 | 建议取舍 |
|---|---|---|---|
| `eveai` | Node.js + TypeScript + Fastify + SQLite，多渠道 Agent | 最接近信息助理总体形态 | 作为总体架构首要参考 |
| `eve-mcp-server` | C#/.NET MCP Server | ESI 分域、工具设计、缓存/限流 | 作为 Connector/Tool 参考 |
| `rift-intel-fusion-tool` | Kotlin/JVM + Compose Multiplatform + SQLite | 实时 Intel、上下文合并、状态和告警 | 吸收领域模型，不直接复制桌面单体 |
| `TheGuideToTheNewEden` | WinUI 3 + .NET 9 + SQLite | 中文用户功能、日志、频道预警、星图 | 作为中文产品功能参考 |
| `PySpy` | Python + wxPython + SQLite | 复制 Local 名单、威胁画像 | 只借鉴 UX/指标，项目已停更 |
| `openclaw-eve-skill` | Python 标准库 + OpenClaw Skill | PKCE、最小 scope、多角色、只读 | 作为权限设计参考 |
| `localisprimary/esi` | TypeScript OpenAPI 代码生成 | 全类型 ESI 客户端、ETag/Cache-Control | 作为 ESI 低层客户端参考 |
| `goesi-upstream` | Go + OpenAPI 生成 | ESI 全覆盖、Context、多 Token | 借鉴 Facade、Context、mock |
| `aladac-sde` | Ruby + Dry::Struct + MessagePack | SDE 懒加载、类型化、更新流水线 | 借鉴数据管线，Node 侧使用 SQLite |
| `Marquette` | Rust + Actix + Yew/WASM + PostgreSQL | 市场、估价、前后端 DTO、增量同步 | 二期市场架构参考 |
| `eve-market-browser` | Go + Remix + SQLite + 监控 | 市场快照、历史、采集管线 | 借鉴采集/查询分层，警惕数据库锁 |
| `eveeyevemaps` | JSON 地图数据资产 | 地图展示布局 | 仅作可选展示资产，注意归因与授权 |

## 2. 关键架构结论

### 2.1 EVE AI Agent：总体骨架

推荐借鉴其单进程形态：

```text
Web / CLI / Telegram / Discord
              ↓
       统一 Chat Pipeline
              ↓
     Agent Runtime + Tools
              ↓
      Domain Services
              ↓
       ESI / SDE / zKill
              ↓
             SQLite
```

值得借鉴：SQLite 单进程、本地 SDE、SSO、多角色、私有数据隔离、工具调用、SSE、请求取消、缓存和数据新鲜度。MVP 不应直接复制其多用户队列、子 Agent、计费和复杂协调器。

### 2.2 RIFT：实时情报领域模型

RIFT 最有价值的是：

```text
日志/剪贴板/ESI/zKill
          ↓
       标准化事件
          ↓
       实体解析消歧
          ↓
       上下文合并
          ↓
       按星系维护状态
          ↓
      风险规则与告警
```

建议采用 `Parser → Entity Resolver → Context Merger → State Store → Rule Engine → Alert/UI` 的流水线。重点保留来源、观察时间、置信度、过期时间、原始证据和人工修正。

不建议直接移植 Compose 窗口、JNA、游戏设置写入、Autopilot、Blue Marshal、XMPP、OpenCV 等与核心信息助理无关的耦合。

### 2.3 ESI 接入

建议采用：

```text
OpenAPI 生成的 ESI Client
             ↓
       ESI Transport
             ↓
         ESI Gateway
             ↓
       Domain Services
             ↓
      Web/Agent/MCP/UI
```

必须内建：

- 产品名、联系邮箱的 User-Agent
- `Cache-Control`、`Expires`、`ETag`、`If-None-Match`
- `429`、`420`、`Retry-After`
- `X-Esi-Error-Limit-Remain/Reset`
- 5xx、超时、网络错误分类
- 请求取消、并发限制和请求去重
- 公共缓存与按角色隔离的私有缓存
- `fetchedAt`、`expiresAt`、`stale`、`source`

低层客户端不应直接暴露给业务。业务层应提供 `getRouteRisk`、`getPilotThreat`、`getPiStatus` 等高层能力。

### 2.4 SSO 和权限

借鉴 OpenClaw：EVE SSO OAuth2/PKCE、最小 scope、按角色 token、默认只读。

建议 Scope Profile：

- `basic`：位置、舰船、技能、技能队列、克隆、植入体、在线状态
- `pi`：basic + 行星开发
- `industry`：basic + 资产、工业、订单、合同
- `full`：钱包、邮件等高敏感数据

Token 应使用 Windows DPAPI/系统钥匙链或 libsodium 加密；模型、前端和日志均不得看到明文 Token。

### 2.5 市场

MVP 先做按需查询：物品、Jita、区域订单、简单估价；二期再做热门区域/热门物品的增量快照和历史。

不要一开始进行全宇宙全量市场高频扫描。采集层、规范化存储、查询 API 和 UI 必须分开，避免 EVE Market Browser 暴露的数据库锁和采集争用问题。

### 2.6 Intel 和威胁

推荐统一事件模型：

```ts
interface IntelEvent {
  id: string
  source: 'clipboard' | 'chat-log' | 'manual' | 'esi' | 'zkill'
  receivedAt: string
  observedAt?: string
  systemId?: number
  systemName?: string
  pilots?: EntityRef[]
  ships?: EntityRef[]
  direction?: string
  status?: 'friendly' | 'neutral' | 'hostile' | 'unknown'
  confidence: number
  expiresAt?: string
  rawText?: string
  evidence: EvidenceRef[]
}
```

威胁评分由确定性规则生成，AI 只负责中文解释：

```text
安全等级 + 近期击杀 + 已知 Camp + Intel 情报 + 敌对存在 + 舰船差距
```

输出必须包含评分因素、证据、来源和数据更新时间，不能只返回黑盒的“危险/安全”。

## 3. 推荐目标架构

### 3.1 MVP 形态

```text
React + TypeScript Web UI
              ↓
         Fastify API
              ↓
        SSE 实时推送
              ↓
 Node.js/TypeScript 单进程
              ↓
 SQLite WAL + 独立只读 SDE SQLite
              ↓
 ESI / zKill / 本地情报导入
              ↓
 Domain Services + 确定性风险引擎
              ↓
 AI Tools + 中文解释
```

### 3.2 推荐代码分层

```text
src/
├── api/              # HTTP、SSE、鉴权边界
├── assistant/        # 对话、意图、工具、引用
├── domain/           # character/market/intel/route/threat/map/alerts
├── connectors/       # esi/sde/zkill/eve-kill/local-files
├── auth/             # SSO、PKCE、Token Vault、scope
├── storage/          # SQLite、migration、repositories
├── jobs/             # 刷新、feed、过期、通知
├── plugins/          # manifest、registry、permissions
└── shared/           # types、errors、freshness、logger
```

### 3.3 统一业务结果

```ts
interface AssistantResult<T> {
  data: T
  sources: SourceCitation[]
  freshness: FreshnessInfo
  warnings: string[]
  confidence?: number
}
```

## 4. 数据库建议

应用库和 SDE 库分离：

```text
sde.sqlite       # 版本化、只读、系统/物品/舰船/蓝图/星门
application.db   # 用户、角色、Token、缓存、Intel、告警、会话
```

应用库主要表：

```text
users
sessions
characters
character_scopes
esi_tokens
oauth_states
esi_cache
intel_reports
intel_entities
intel_state
intel_evidence
killmails
killmail_participants
alert_rules
alert_deliveries
conversation_threads
messages
tool_audit
sync_jobs
```

所有动态数据应带 `source`、`fetched_at`、`expires_at`、`confidence` 或 `warnings`。

## 5. 技术路线与 MVP 分期

### MVP-0：可靠数据底座

- React Web
- Fastify
- SQLite WAL
- migrations
- SDE 导入和搜索
- 系统/物品/舰船查询
- ESI status/universe
- 中文术语映射
- 来源/时间显示

### MVP-1：角色信息

- EVE SSO/PKCE
- 角色绑定
- 位置、舰船、技能队列、在线状态
- 克隆、植入体
- 可选 PI
- 基础自然语言问答

### MVP-2：情报和路线

- zKill/EVE-KILL 画像
- 剪贴板批量分析
- 情报文本粘贴
- 系统/舰船/角色识别
- 路线和风险
- Web 告警

### MVP-3：市场和地图

- 物品和区域订单
- Jita 价格
- 简单估价
- 历史价格
- 星图和风险地图
- 实时击杀热度

### MVP-4：桌面和外部渠道

- Wails 桌面壳
- 系统托盘和置顶窗口
- 剪贴板快捷分析
- 系统通知
- Telegram/Discord
- MCP Adapter
- 插件 SDK

## 6. 框架选择建议

### 总体结论

**已确定：React + TypeScript 作为 UI，Go 作为核心服务，Wails 作为桌面壳。**

不要让 Wails 决定领域架构。Go 核心应保持可独立运行的 application/service 层；Wails 负责桌面生命周期、系统能力和前端桥接，同时保留 HTTP API，以支持浏览器、CLI、Telegram、Discord 和 MCP。

### 框架对比

| 方案 | 适合度 | 优点 | 主要问题 | 结论 |
|---|---:|---|---|---|
| Electron | 高 | 生态最大、开发快、Chromium 一致、剪贴板/托盘/通知成熟 | 体积和内存高、进程多、常驻资源较大 | 适合快速桌面 MVP，不是首选长期壳 |
| Tauri | 很高 | 体积小、内存低、Web UI 复用、系统能力够用、安全边界较清晰 | Rust side 学习成本、插件生态小于 Electron、复杂原生能力需自己处理 | **首选桌面壳** |
| Wails | 中高 | Go 后端简单、前端 Web 复用、包体较小 | Go/前端桥接生态和资料少于 Tauri/Electron；Windows/跨平台细节需验证 | 团队 Go 很强时考虑 |
| React Native | 中低 | 移动端生态好、可复用 React 思维 | 桌面端不是主场；Windows/macOS/Linux 桌面支持分散；地图/托盘/日志能力需原生桥接 | 不建议作为本项目主框架 |
| Flutter | 中高 | 跨平台 UI 一致、桌面支持成熟、渲染和复杂列表好 | Dart 生态与 Web/Node 分裂；插件质量不一；Web 与桌面复用不如 React | 适合纯桌面/移动产品，不适合 Web-first |
| Kuikly | 低 | 跨端理念、中文团队可能熟悉 | 生态、桌面端成熟度、第三方库和长期维护风险较高 | 不建议用于此类基础设施型应用 |
| Kotlin + Compose Multiplatform | 中高 | RIFT 已验证桌面能力；状态/协程/DI/原生桌面体验好 | JVM/Java 运行时和构建链较重；Web 与桌面复用路线不如 React；开发成本较高 | 纯桌面优先时可选，不建议 Web-first |
| .NET MAUI/Avalonia | 中高 | C# 桌面能力、Windows 集成、团队人才多 | Web 复用弱；跨平台 UI 生态和细节需评估 | 已有 .NET 团队时考虑 |

### 选择依据

目标应用同时需要：

- Web 工作台
- 地图、市场、情报实时流
- 本地 SQLite
- 剪贴板
- 系统通知
- 后续 Telegram/Discord/MCP
- Windows/macOS/Linux 可选支持
- 快速迭代中文 UI

因此应把“核心服务”和“UI 外壳”分开：

```text
核心：Go + SQLite
Web/桌面 UI：React + TypeScript + Vite
桌面封装：Wails
实时：SSE（必要时 WebSocket）
扩展：MCP/Plugin Adapter
```

### 为什么选择 Go + Wails

Go 负责 ESI Gateway、SDE、SQLite、后台任务、Intel、路线、风险和通知调度；React 负责页面与状态；Wails 只负责桌面窗口、托盘、剪贴板、系统通知、快捷键和前端绑定。核心 Go 服务必须能够脱离 Wails 运行，保留 HTTP API，避免未来接入 Web、CLI、Telegram、Discord 和 MCP 时被桌面框架锁定。

建议职责边界：

```text
Go Core
├── domain/       # 领域模型与规则
├── connectors/   # ESI、zKill、SDE、本地日志
├── storage/      # SQLite、迁移、Repository
├── jobs/         # 刷新、Feed、过期、通知
├── assistant/    # AI 工具和对话编排
└── transport/    # HTTP API、Wails bindings

React Frontend
└── 地图、市场、Intel、角色中心、聊天和设置
```

### 为什么不是 Electron

Electron 完全可行，尤其适合快速做出第一个桌面版本，但本项目会长期运行后台监听、缓存、地图和通知。Electron 的 Chromium + Node 常驻资源偏高，且安全边界需要严格处理。若只是验证产品，Electron 可以作为短期方案；若准备长期发布，Tauri 更合适。

### 为什么不是 Flutter/Kotlin/React Native

这些方案更适合“单一跨平台客户端”，而本项目的核心难点不在 UI 绘制，而在：

- ESI 数据治理
- SSO/Token 隔离
- Intel 事件状态
- 本地日志和剪贴板输入
- 规则风险引擎
- Web、桌面、机器人、多渠道复用

因此不应把核心绑定在 UI 框架上。React Web + 本地服务可以最大化能力复用。

## 7. 安全与合规红线

必须做：

- OAuth state 一次性、短时、绑定会话
- PKCE
- 最小 scope
- Token 加密保存
- HttpOnly/Secure/SameSite Cookie
- CSRF、CSP、Origin 校验
- 角色和私有缓存隔离
- 敏感日志脱敏
- 数据删除和授权撤销
- 遥测默认关闭或 opt-in

明确不做：

- 不读游戏进程内存
- 不注入 DLL
- 不模拟鼠标键盘
- 不自动驾驶
- 不自动下单
- 不自动移动资产
- 不自动发送邮件
- 不把 Token 交给模型

## 8. 手机端与公网通信架构

新增需求：提供手机端应用，用于接收提醒、查看摘要，并通过公网服务器与桌面端应用通信。此时不应让手机直接连接桌面端，也不应把桌面端端口暴露到公网。

### 8.1 推荐拓扑

```text
                    HTTPS / WSS
┌─────────────┐  ┌──────────────────┐  ┌─────────────────┐
│ 手机 App    │◄─┤ 公网服务端（Server） │◄─┤ Wails 桌面端   │
│ Flutter     │  │ Go API + WS      │  │ Go Core         │
└──────┬──────┘  └────────┬─────────┘  └────────┬────────┘
       │                   │                     │
       │ FCM/APNs 推送     │ 鉴权/设备/消息路由   │ 本地 Intel/ESI
       └───────────────────┴─────────────────────┘
```

公网服务器只负责：

- 用户/设备身份认证
- 桌面端在线连接管理
- 手机与桌面端消息中继
- 告警事件持久化和未读状态
- 推送通知任务
- 最小化的同步数据

公网服务器不应负责：

- 保存桌面端 EVE Token
- 直接调用桌面端本地文件
- 直接执行游戏相关操作
- 默认保存完整聊天日志、资产和钱包数据
- 作为所有 ESI 数据的永久仓库

### 8.2 桌面端连接方式

桌面端主动向公网服务器建立出站 WSS 长连接：

```text
桌面端 ──主动出站 WSS──> server.example.com
```

不要采用公网服务器反向连接桌面端。这样可以避免：

- 家庭网络端口映射
- 暴露桌面 IP
- NAT/防火墙问题
- 桌面端公网监听风险

桌面端网络断开后应自动重连，并使用指数退避和随机抖动。每条消息需要 `messageId`、`deviceId`、`createdAt`、`type` 和幂等处理。

### 8.3 通信协议

建议统一使用版本化 JSON 信封：

```json
{
  "version": 1,
  "messageId": "uuid",
  "conversationId": "optional",
  "sender": "desktop",
  "type": "intel.alert",
  "createdAt": "2026-08-23T10:20:00Z",
  "payload": {
    "title": "2 跳发现敌对情报",
    "severity": "high",
    "systemId": 30000142,
    "summary": "过去 5 分钟内有 3 名敌对飞行员经过"
  }
}
```

消息类型至少包括：

```text
device.register
presence.update
intel.alert
intel.summary
route.request
desktop.status
sync.request
sync.result
ack
error
```

默认只同步业务摘要和引用，不同步原始 Token。对每个消息执行：

1. 服务器鉴权
2. schema 校验
3. 权限校验
4. 大小限制
5. 幂等去重
6. 投递或离线存储
7. ACK/失败重试

### 8.4 手机端功能范围

第一版手机端应保持轻量：

- 登录和设备绑定
- 接收高优先级告警
- 查看 Intel 摘要
- 查看桌面端在线状态
- 查看角色/路线摘要
- 暂停、启用和确认告警
- 查看最近事件
- 向桌面端发送有限的只读查询请求

不建议第一版在手机端实现完整地图、完整市场和完整角色工作台，也不建议手机直接持有 EVE SSO Token。

### 8.5 推送策略

实时数据通道和系统推送必须分开：

```text
桌面在线 + 手机 App 前台：WSS 实时消息
手机 App 后台或桌面离线：FCM/APNs 推送
手机打开后：HTTPS 拉取离线事件和最新状态
```

建议：

- Android 使用 FCM
- iOS 使用 APNs
- 推送内容只放摘要，不放敏感资产、钱包或邮件正文
- 用户可按严重级别、系统、角色和时间段配置推送
- 推送必须去重、限频，并支持确认/已读

### 8.6 服务端建议

公网服务端仍然采用 Go，与桌面端共享协议和领域 DTO，但不要直接共享桌面端数据库访问代码：

```text
relay-server/
├── auth/          # 用户、设备、短期会话
├── devices/       # 桌面/手机设备注册和撤销
├── transport/     # WSS 连接、路由、ACK、重试
├── notifications/ # FCM/APNs、去重、限频
├── events/        # 最小化事件和未读状态
├── api/           # HTTPS REST
└── storage/       # PostgreSQL 或 SQLite（按规模选择）
```

早期个人使用可以使用 Go + SQLite；公网长期运行、需要多用户和可靠离线投递时，推荐 PostgreSQL。桌面端继续使用本地 SQLite，不与公网数据库共享。

### 8.7 认证与安全

推荐设备模型：

```text
用户登录
  ↓
手机生成设备密钥对
  ↓
短期配对码/二维码
  ↓
桌面端确认配对
  ↓
服务器绑定 userId + deviceId + publicKey
  ↓
签发短期 access token + refresh token
```

必须具备：

- HTTPS/WSS only
- 短期 Access Token、可撤销 Refresh Token
- 设备级密钥和设备撤销
- 桌面端与手机端分离权限
- 消息 schema 和大小限制
- 每用户连接数、消息数、推送数限流
- 重放保护（nonce/messageId/时间窗口）
- 服务器不记录 Authorization、EVE Token 和原始私密 payload
- 管理后台和服务端日志脱敏

公网服务器即使被攻破，也不应获得桌面端 EVE SSO Token；EVE Token 仅保留在桌面端本地 Token Vault 中。

### 8.8 手机框架选择

新增手机端后，推荐优先考虑 **Flutter**，桌面端仍使用 **Go + Wails + React**：

| 手机方案 | 建议 |
|---|---|
| Flutter | 手机端首选；推送、后台生命周期和原生能力成熟，适合轻量告警 App |
| React Native | 如果希望最大化 React/TypeScript 复用可以选择，但推送、后台连接和原生桥接需要更多验证 |
| Kotlin Multiplatform | 适合已有 Kotlin 团队，工程质量高，但与 Wails/React 技术栈分裂 |
| Kuikly | 当前不建议作为关键通信客户端的基础设施 |

手机端不需要复用桌面端 UI 代码；应复用版本化协议、OpenAPI/JSON Schema 和产品语义。综合可靠性，建议：

```text
桌面：Go + Wails + React
手机：Flutter
公网：Go Server
协议：HTTPS REST + WSS + FCM/APNs
```

### 8.9 推荐实施顺序

1. 桌面端本地告警事件模型和 Outbox
2. Go Server 的用户/设备/配对
3. 桌面端出站 WSS、ACK、重连和离线补发
4. 手机端登录、设备绑定、事件列表
5. Android FCM 推送
6. iOS APNs 推送
7. 手机向桌面端发送有限只读请求
8. 多桌面、多手机、权限和设备撤销

关键设计是 **Outbox + ACK + 幂等**：桌面端先把待发送事件写入本地 outbox，服务器确认后再标记完成；服务器为离线手机保存有限事件，手机收到后 ACK。这样断网、重启和重复投递都不会丢失告警。

## 9. 最终一句话建议

构建一个“懂中文、只读安全、证据可追溯、数据留本地”的 EVE 信息工作台：

> **核心采用 Go + SQLite；前端采用 React + TypeScript；桌面端采用 Wails。Go 核心保持独立可测试，Wails 只承担桌面壳与系统能力。**

参考项目只提供设计经验，不应作为运行时依赖直接拼接。

## 9.1 桌面信息架构与视觉重构（2026-09-16）

现有桌面实现功能已显著超过早期 Intel 输入页，但界面仍带有明显的生成式 AI 仪表板特征：大标题、卡片墙、装饰性英文 kicker、emoji/字符图标、过多 pill 和重复状态卡。这会降低桌面数据工具的扫描效率。

参考 Wails Showcase 中 Kafka-King 这类数据库/消息队列客户端，桌面端调整为：

```text
┌───────────────┬──────────────────────────────────────────────┐
│ 文本侧栏      │ 紧凑工具栏 / 当前角色 / 连接与新鲜度       │
│ Overview      ├──────────────────────────────────────────────┤
│ Character     │ Tab / Filter / Action Bar                    │
│ Economy       ├──────────────────────┬───────────────────────┤
│ Market        │ 主数据表格           │ 详情/检查器           │
│ Intel         │                      │                       │
│ Alerts        ├──────────────────────┴───────────────────────┤
│ Agent         │ 状态栏：Relay / ESI / Logs / SDE / Sync     │
│ Settings      │                                              │
└───────────────┴──────────────────────────────────────────────┘
```

约束：

- 优先 1366×768，允许密集信息而非放大留白；
- 侧栏约 200–220px，使用清晰文本标签和一致的线性图标，不使用 emoji；
- 控件约 30–34px，高度、4–6px 圆角、13px 主字号；
- 页面以表格、树、tab、split pane、inspector 为核心，卡片只保留 KPI 摘要；
- 无渐变、无玻璃拟态、无夸张阴影和营销语言；
- 颜色用于状态和选择，不用于装饰；
- 所有远程数据明确显示 fetched/expires/stale/source；
- 缺 scope、未授权、过期、刷新中、部分失败必须成为一等状态；
- Character/Economy 是账户隔离私有数据；Market/Universe 是共享公共缓存；
- 桌面只持有 Alice Session，不持有 EVE access/refresh token。

一级信息架构：

1. **Overview**：Relay、EVE、Logs、SDE、同步状态及重要摘要；
2. **Character**：身份、在线、位置、舰船、技能和技能队列；
3. **Economy**：钱包、资产、个人订单以及后续合同；
4. **Market**：公共价格、区域订单、历史、价差与类型/星系 Resolver；
5. **Intel**：Chat/Game 实时采集、结构化事件和手工调试入口；
6. **Alerts**：告警和投递状态；
7. **Agent**：只读查询会话，降级为辅助入口而非产品主视觉；
8. **Settings**：服务器、授权、日志目录、SDE、隐私和数据管理。

Superdesign 设计项目：`Alice-EVE Desktop Command Desk`。已建立当前 UI 基线，并生成两条可评审分支：紧凑浅/石墨 Command Console 与深色 Operations Terminal。实施前应选择一条方向，避免直接在旧卡片墙上继续叠加。

## 10. 数据平台架构与实施总计划（2026-09-16 当前基线）

### 10.1 产品目标校正

Alice-EVE 不是“聊天文本粘贴解析器”，也不能按空页面逐个补数据源。产品应先建立完整、可追溯、可扩展的数据底座，再在其上提供情报、市场、角色、告警、Agent 和移动端能力。

正式数据面由四层组成：

```text
本机实时层：Chatlogs + Gamelogs + 客户端 Session
授权私有层：EVE SSO + ESI 角色私有数据
公共在线层：ESI 公共 Universe/Market/Status + 可选 EVE-KILL/zKill
静态知识层：EVE SDE（类型、星系、星门、蓝图、市场组）
                         ↓
              统一规范化事件/实体模型
                         ↓
          本地 SQLite + 服务端 PostgreSQL/共享缓存
                         ↓
       情报、市场、角色中心、风险、告警、Agent、移动端
```

### 10.2 对 RIFT 的完整吸收范围

RIFT 的价值不只是 Chatlogs。当前应明确吸收以下模式：

- `ChatLogsObserver`：目录发现、文件轮转、按字节 offset 增量读取、频道 Header、消息去重；
- `GameLogsObserver` / `GameLogWatcher`：战斗、反跳、通知、采矿、登录 Session 等即时事件；
- 日志目录探测：Windows Known Documents、普通 Documents、OneDrive，以及用户手动覆盖；
- 最新角色/频道日志选择、overflow 后重扫、截断恢复；
- Intel 标准化、实体消歧、上下文合并、过期和告警规则；
- ESI/SDE/击杀/市场与本地实时事件的合并视图。

不引入读游戏内存、DLL 注入、输入模拟或自动游戏行为。

### 10.3 `EveClient.json` 的角色和边界

本地忽略文件 `EveClient.json` 是 EVE Developer Portal 应用配置，包含：

- `clientId`：公开应用标识；
- `clientSecret`：仅服务端可用的 confidential-client 凭据；
- `callbackUrl`：EVE SSO HTTPS 回调；
- `scopes`：应用可请求的 ESI 权限全集。

它不是运行时数据缓存，也不会自动让系统获得所有 ESI 数据。真正启用某项能力需要：授权请求包含 scope、用户同意、服务端安全保存 grant/refresh token、同步器调用对应 ESI Endpoint、数据规范化与持久化。

安全约束：该文件保持 Git ignored；不得进入桌面 bundle、浏览器、移动端、日志或模型上下文；Client Secret 只通过服务端 Secret/环境变量提供。

### 10.4 OAuth/ESI 持续授权模型

现有 EVE SSO 已能证明角色身份并创建 Alice 账号，但持续 ESI 同步需要升级为 authorization grant：

```text
accounts
characters
character_oauth_grants       # provider、character、加密 refresh token、scope、状态
character_scope_snapshots
esi_sync_jobs
esi_sync_cursors
esi_cache_metadata
```

建议采用服务端加密保存 EVE refresh token，因为手机共享、桌面离线后台同步和服务端公共缓存需要持续授权。具体实施必须满足：

- refresh token 使用独立主密钥 envelope encryption（密文、nonce、key version）；
- Access Token 仅短时驻留内存，不作为业务数据持久化；
- 每角色隔离，scope 快照可审计；
- 支持撤销、重新授权、scope upgrade 和删除账号数据；
- Token、authorization code、client secret 不进入 URL、业务日志和 Agent；
- 服务端只向客户端暴露规范化数据，不暴露 EVE Token。

这取代前文“公网服务端绝不保存任何 EVE Token”的早期方案。新的边界是：**服务端可以保存加密 EVE refresh token，但不得将其暴露给桌面、手机、浏览器或模型；是否启用持续同步由用户明确授权。**

### 10.5 Scope Profile（避免一次请求全部权限）

`EveClient.json` 列出的 scope 是能力全集，不应默认全部请求。按功能分包并渐进授权：

| Profile | 数据 | 主要 scopes |
|---|---|---|
| `identity` | 身份、在线、位置、舰船 | `publicData`、location/online/ship |
| `character` | 技能、队列、克隆、植入体、忠诚点 | skills/clones/loyalty |
| `economy` | 钱包、资产、个人订单、合同 | wallet/assets/character orders/contracts |
| `combat` | 击杀邮件、通知、联系人/standings | killmails/notifications/contacts/standings |
| `industry` | 蓝图、工业、采矿、PI | blueprints/industry/mining/planets |
| `social` | 邮件、日历、舰队 | mail/calendar/fleet |
| `corporation` | 军团资产、钱包、订单、工业等 | corporation scopes；还需校验角色权限 |

首批建议实现 `identity + character + economy + combat` 的只读子集；邮件正文、写入类 scope、舰队写入、联系人写入和 fitting 写入不进入默认授权。

### 10.6 数据源清单与职责

#### 本地实时源

| 源 | 采集 | 默认隐私策略 |
|---|---|---|
| Chatlogs | 频道、发言者、消息、Listener、Session | 原文只留本机；指定 Intel 频道才产生结构化事件 |
| Gamelogs | combat/notify/mining/bounty/session | 原文只留本机；高风险事件可按规则告警 |
| 剪贴板/手工输入 | 备用导入与调试 | 默认本地，不作为主流程 |

#### ESI 私有源（按角色）

首批：identity、online、location、ship、wallet、skill queue、assets、personal orders、killmails、notifications。随后再做 contracts、industry、mining、PI、mail/calendar/fleet 和 corporation。

#### ESI 公共源（共享缓存）

- status、universe types/systems/stargates/routes；
- public character/corporation/alliance；
- region orders、market history、prices；
- sovereignty/wars/incursions 等按产品需求加入。

#### SDE

使用独立版本化只读数据库，提供 type/system/group/category/market group/blueprint/map topology。应用数据库不得把 SDE 当作无结构 JSON 搜索目录长期使用。

### 10.7 统一数据模型

核心模型至少包括：

```text
SourceRecord       # source、source key、observed/fetched/expires、raw reference
NormalizedEvent    # chat/combat/location/kill/market/order/notification
EntityRef          # character/corporation/alliance/system/type/structure
IntelFinding       # stance、confidence、evidence、TTL
MarketSnapshot     # region/structure/type、bid/ask/history、freshness
CharacterSnapshot  # online/location/ship/wallet/skills 等分域快照
AlertCandidate     # 规则命中但尚未投递
AlertDelivery      # durable delivery + ACK
```

每条业务结果必须携带：`source`、`observedAt/fetchedAt`、`expiresAt`、`stale`、`evidence` 和隔离键（account/character）。原始 Chatlogs/Gamelogs 不上传；结构化事件是否上传由规则与用户设置决定。

### 10.8 ESI Gateway 必备机制

- 产品 User-Agent 和联系方式；
- OAuth Token refresh single-flight；
- `ETag` / `If-None-Match` / `Expires` / `Cache-Control`；
- 分页、请求去重和 per-character concurrency；
- `420/429`、`Retry-After`、ESI error-limit remain/reset；
- 4xx 权限/授权错误与 5xx/网络错误分类；
- endpoint-specific freshness 和退避；
- 公共缓存共享、私有缓存按 account/character 隔离；
- 审计只记录 endpoint、状态、延迟和缓存命中，不记录 Token 或敏感正文。

### 10.9 市场基础层

市场必须分三类：

1. 公共区域市场：region orders、market history、adjusted/average prices；服务端共享缓存；
2. 角色个人订单：需要 `esi-markets.read_character_orders.v1`；按角色隔离；
3. 建筑市场：需要 `esi-markets.structure_markets.v1` 且角色有建筑访问权。

首期不做全宇宙全量高频抓取。优先交易中心和用户查询类型，使用按需缓存；后续再建立热门 region/type 增量快照、估值和价格告警。

### 10.10 存储边界

```text
桌面 SQLite
├── log_sources / log_files / log_cursors
├── normalized_local_events
├── local_intel / local_alert_rules
├── outbox / sync_cursor / pairing_state
└── UI preferences / selected channels

服务端 PostgreSQL
├── accounts / characters / devices / sessions
├── encrypted_character_oauth_grants / scopes
├── esi_sync_jobs / cursors / snapshots
├── shared_public_cache / market_snapshots
├── normalized_remote_events / alerts / deliveries
├── conversations / runs / run_events
└── audit metadata（不含敏感正文和 Token）

独立 SDE SQLite
└── types / groups / categories / systems / gates / blueprints / market groups
```

### 10.11 实施里程碑与验收门槛

#### Phase 0：冻结架构与迁移计划

- 更新本文与工作记录；
- 建立数据源、scope、隐私和存储矩阵；
- 不再把手工输入当主流程；
- 验收：文档无互相冲突的当前路线，安全边界明确。

#### Phase 1：本地采集底座

- Chatlogs + Gamelogs；
- Windows Known Documents/OneDrive/手动目录；
- UTF-8/UTF-16LE、半行、轮转、截断、锁文件读取；
- 持久游标、多角色/频道 metadata、结构化事件、去重；
- 数据源健康页和最近事件；
- 验收：真实 EVE 客户端聊天与战斗各产生一条事件，重启不重复。

#### Phase 2：SDE 正式化

- 导入标准 EVE SDE 到版本化只读 SQLite；
- type/system/ship/market group/blueprint/map topology repository；
- 校验和、版本切换和原子更新；
- 验收：任意真实 type/system ID 可本地解析，日志实体不依赖硬编码列表。

#### Phase 3：持续 ESI 授权与身份域

- PostgreSQL grant/schema migration；
- refresh token 加密、refresh single-flight、scope 快照/升级/撤销；
- identity/online/location/ship/skills/queue/clones；
- 验收：桌面关闭后服务端仍能按 ESI 缓存规则同步；撤销后停止且数据可删除。

#### Phase 4：经济与战斗数据

- wallet/assets/personal orders/contracts；
- killmails/notifications/contacts/standings；
- 角色快照 API 和 UI；
- 验收：数据来源、新鲜度、角色隔离和权限错误清晰可见。

#### Phase 5：公共市场与 Universe

- 公共 market/universe Gateway；
- Jita/主要区域按需订单、历史、价格和估值；
- 共享缓存、请求合并、错误预算；
- 验收：市场查询不依赖角色 Token，缓存和数据时间可见。

#### Phase 6：Intel/风险/告警闭环

- local + ESI + kill + market 的统一事件；
- Parser → Resolver → Context Merger → State Store → Rule Engine；
- 指定频道自动结构化、战斗高风险规则、显式/自动发布选项；
- WSS durable delivery/ACK、手机提醒；
- 验收：证据可追溯，断网重连不丢失，重复事件不重复告警。

#### Phase 7：高级域与 Agent

- industry/mining/PI/mail/calendar/fleet/corporation；
- Agent conversation 自动创建、工具权限和只读执行；
- 验收：每个工具声明 scope、来源、新鲜度和审计记录；默认无写操作。

### 10.12 当前优先级

在 Phase 1–3 完成前，不优先扩展空壳 UI、Agent 演示页或更多手工输入功能。近期执行顺序固定为：

```text
完成文档计划
→ 收敛/测试 Chatlogs + Gamelogs 统一采集器
→ 建立正式 SDE
→ 实现持续 ESI Grant 与同步 Gateway
→ 角色/经济/战斗数据
→ 公共市场
→ Intel/告警/Agent
```

每一 Phase 必须先有 migration、repository、测试和数据健康指标，再接 UI。
## 11. 全区域贸易调度系统架构（实施基线）

> 本节是 Alice-EVE 贸易系统的当前实施基线。它取代“只比较五大贸易中心”的早期设计，但保留五大中心作为高优先级扫描与筛选预设。系统坚持只读：自动采集、分析、组单、规划、复核和监控，不自动下单、不自动修改市场订单，也不自动设置航点。

### 11.1 产品目标与边界

系统从“市场机会扫描器”升级为“有状态的运输计划工作台”。它使用全区域公开订单、区域历史、SDE 宇宙拓扑和已授权角色快照，在资金、货舱和路线安全约束下生成三类方案：

1. 单品单程：一个采购地点到一个销售地点；
2. 组合装载：同一趟运输装载多种物品；
3. 链式计划：最多四个停靠点、三段航程，可在中途卸货并顺路采购。

第一阶段包括公开 NPC 站点和区域订单中可公开识别的地点。玩家建筑市场必须另设授权、ACL、停靠权、结构税和审计边界，不与公共市场静默混合。市场历史是区域级而非站点级；预计需求、售罄时间和成交概率必须标注为派生估算。

### 11.2 核心领域模型

```text
TradePlan
├─ id / version / state
├─ accountId / characterId
├─ ConstraintSet
├─ CharacterContext
├─ MarketSnapshotRef[]
├─ Stop[]
├─ Leg[]
├─ LoadItem[]
├─ ProfitBreakdown
├─ ConfidenceBreakdown
└─ Validation / ChangeLog
```

- `ConstraintSet`：采购预算、现金预留、可用货舱、最低安全等级、最大跳数、最大停靠数、最大单品/单站资金占比、税费、运费、规划模式。
- `CharacterContext`：当前角色、位置、星系、站点/结构、当前舰船、货舱容量、钱包余额，以及每个值的来源、抓取时间和是否为估算。
- `MarketSnapshotRef`：区域、批次、快照版本、覆盖率、抓取/到期时间、缺页/错误状态。
- `Stop`：停靠地点、装载/卸载动作、到达前后资金和货舱。
- `Leg`：起终点、路径星系、跳数、最低安全等级、低安/零安跳数、路线策略。
- `LoadItem`：Type、中文/英文名、源/目标订单、数量、VWAP、体积、资金占用、航段占舱区间、边际利润。
- `TradePlan.state`：draft、optimizing、needs_review、validated、executing、stale、invalid、completed。

### 11.3 全区域市场数据管线

```text
ESI region types / orders / history
            ↓
区域分片调度器（优先级 + jitter）
            ↓
受限分页采集（页数/行数/字节/时长/并发）
            ↓
collection_run 临时批次
            ↓ 全部页面成功
规范化订单 + 原子快照发布
            ↓
中心/站点报价、订单深度、候选边预聚合
            ↓
单品、组合、链式规划
```

采集原则：

- 服务端采集全区域，客户端绝不下载原始全集；
- 先获取区域活跃 Type，再按区域/Type 分片，避免长期使用无 Type 过滤的巨大响应；
- 热门 Type、用户监控项、当前位置附近区域和计划涉及订单优先；
- 全局 semaphore、令牌桶、`Retry-After`、ESI error-limit remain/reset、指数退避和 jitter；
- 每批设置最大页数、行数、字节和执行时间；
- 分页批次全部成功后原子发布，失败时保留上一完整快照，绝不发布半快照；
- 快照记录覆盖率、页数、行数、耗时、最老数据年龄和缺失区域；
- 订单按 `order_id` 去重，冲突数据不得累加数量；
- 热订单短 TTL，偏远低竞争订单可使用较长半衰期；
- 原始订单短期保留，行情聚合和计划引用保留更久，并实施明确保留策略。

建议表：

```text
market_collection_runs
market_snapshots
market_orders_current
market_quote_history
market_region_coverage
market_watchlists / market_watchlist_items
trade_plans / trade_plan_versions（后续持久化）
```

### 11.4 角色、舰船、资金与货舱

角色上下文来自已授权快照：location、ship、wallet、assets/fittings/skills（可用时）。采购预算与钱包余额严格分离：

```text
最大可用金额 = 钱包余额 - 用户保留金额 - 税费缓冲 - 已占用资金
本趟采购预算 ≤ 最大可用金额
```

默认保留 10% 流动现金；单品最多占预算 25%，单站最多占 40%，均可配置。

当前舰船 Type 可由 ESI 获取，基础普通货舱由 SDE/Dogma 推导。若装配、技能、容器或特殊货舱数据不足，必须返回 `estimated=true` 和来源，要求用户确认后才能进入 `validated`。第一阶段仅使用普通货舱，不把矿石舱、弹药舱、行星货舱、无人机舱或舰船维护舱误算为通用容量。

### 11.5 路线与安全约束

截至 2026-09-19，服务端路线已抽取为 `internal/routeplanner` 可复用模块，供市场及后续合同、资产搬运和工业物流调用。模块按 active SDE build 缓存不可变邻接图，提供单条和最多 500 条批量路线 API；结果缓存按 SDE version 与完整约束隔离。SDE 切换只在新图完整加载成功后发布，并使旧结果失效。不可达必须返回 `unavailable`，不得回退为零跳；同星系不同空间站则是合法零星门跳。

当前生产市场规划使用满足最低安全等级的有界最短路径。下面的安全优先多目标路线和指定星系避让仍是算法扩展目标，不应在 UI 中宣称已经实现。

宇宙图来自 SDE stargates/systems。规划器目标支持：

- 最短路线；
- 满足最低安全等级的最短路线；
- 安全优先路线（安全惩罚加权）；
- 避开指定星系；
- 返回完整星系列表、跳数、最低安全等级、低安/零安跳数。

最低安全等级是硬约束。违反路线不能进入可执行推荐，只能进入排除原因列表。安全等级来自 SDE/ESI，不等价于实时营地或击杀危险度；若没有第三方/本地击杀模型，UI 必须显示“实时危险度未知”。

### 11.6 订单可执行性与财务公式

源端按卖单价格升序、目标端按可达买单价格降序逐档撮合，使用 VWAP，不只比较顶部报价。目标买单必须验证 `station`、`solar_system`、跳数范围或 `region` 覆盖；无法验证的订单不得进入立即成交榜单。

```text
可执行数量 = min(期望量, 源端深度, 目标端有效深度, 预算容量, 货舱容量)
净利润 = 销售收入 - 采购成本 - 销售税 - 经纪费 - 运输成本 - 风险成本
净利率 = 净利润 / 落地成本
```

VWAP 已包含订单簿滑点，不得再次重复扣除。输出还包括：单趟净利润、ISK/m³、ISK/跳、ISK/m³/跳、预算占用、装载率、预计消化时间、压力测试利润。

压力测试至少包含：卖价下降 2%/5%、仅售出 50%、顶部目标买单消失。金额持久化和最终结算使用 NUMERIC/定点数，不以二进制浮点作为财务事实来源。

### 11.7 需求、流动性、评分与置信度

历史需求使用最近 30 个有效交易日，至少 14 天；对成交量进行 5%–95% Winsorization。区域历史不能伪装成站点级成交量。

基础评分建议：盈利质量 35%、需求 20%、流动性 15%、执行/滑点 10%、路线安全 10%、资本效率 10%。硬门槛先于评分：深度覆盖、净利润、净利率、历史完整度、行情年龄、需求占比和安全约束。

评分与置信度分离：

```text
综合评分：机会本身的风险调整质量（0–100）
置信度：数据新鲜度、深度、历史、价格稳定、路线完整度、费用来源和执行复杂度
```

每个计划必须解释“为什么选择、为什么没选次优、最敏感假设、可能失败的位置”。

### 11.8 组合装载算法

第一阶段组合限制为一个采购站、一个销售站、多物品。候选先按安全、时效、订单范围和净利润硬过滤，再在预算与货舱双约束下使用确定性受限背包/启发式求解。

目标可切换：最大净利润、最大 ISK/m³、最大 ISK/跳、快速售罄、平衡。必须防止单品和单站过度集中，记录每个物品的边际贡献，并允许移除低贡献物品后增量重算。

### 11.9 链式计划算法

第一阶段边界：最多 4 个停靠点、3 个航段，不重复访问同一站，不重复消费同一订单深度。使用 Beam Search：

1. 从当前位置构造满足安全约束的采购边；
2. 更新现金和货舱状态；
3. 扩展可出售/顺路采购的下一站；
4. 每一步重新计算资金、货舱、路径和订单剩余深度；
5. 用风险调整净利润、收益密度、空驶、复杂度和置信度剪枝；
6. 保留有限 Beam，避免组合爆炸和结果生成期间行情过期。

输出 Pareto 推荐：最高利润、最高利润率、最安全、最短、最充分装载。链条中断时提供就地清仓、跳过后续站、从当前位置重规划三种补救及预计损失。

### 11.10 快照、复核与执行状态

计划绑定不可变 Market Snapshot ID 和约束版本。参数变化不得静默修改已有计划，只能标记 `needs_review` 并生成新版本。

“加入计划”与“开始执行”分离。开始执行前重新验证：位置、舰船、货舱、资金、订单存在性、价格/数量偏移、路径安全和预计净利润。结果返回仍有效、缩量、替换订单、删项、重新配载或重排停靠等修复动作。

系统保持只读；执行模式只提供逐站采购/出售清单、复制清单、路线说明和监控，不自动下单、挂单或设置航点。

### 11.11 桌面信息架构

沿用方案 B 的 Kafka-King/数据库终端风格，但升级为四阶段工作台：扫描、组单、路线、监控。

```text
数据健康/覆盖率/快照时间/角色状态
预算/预留/舰船/货舱/最低安全等级/规划模式
┌ 全区域候选池 ──────────┬ 当前运输计划 ─────────────┐
│ 单品/组合/链式分视图    │ 预算与货舱进度            │
│ 净利润、ISK/m³/跳       │ 装载货篮与边际贡献        │
│ 距离、安全、时效        │ 多站装卸时间线/站点泳道   │
│ 需求、流动性、置信度    │ 分段现金流/压力测试       │
├─────────────────────────┴───────────────────────────┤
│ 排除原因/缺失区域/规划器状态/恢复动作               │
└─────────────────────────────────────────────────────┘
```

颜色只表达风险、异常和状态，不用满屏红绿表示收益。表格使用虚拟化、服务端筛选/排序和稳定行 ID；订单深度和历史按需加载。地图是辅助视图，默认使用可扫描的时间轴和货物泳道。

### 11.12 API 边界

公开市场行情不携带 Alice 或 EVE Bearer；用户配置、角色上下文和计划必须 Alice 鉴权并按 `account_id`/`character_id` 隔离。

建议 API：

```text
GET  /market/coverage
GET  /market/locations/search
POST /trade/candidates/search
POST /trade/plans/compose
POST /trade/plans/chain
POST /trade/plans/{id}/validate
POST /trade/plans/{id}/repair
GET  /trade/plans/{id}
GET  /trade/plans/{id}/details
```

所有请求限制候选数、区域数、最大链长、时间窗和响应大小；禁止将任意 ESI 路径做成开放代理。规划分两阶段：1–2 秒内返回前 20 个可行方案，后台精排后通过稳定 ID 差量更新。

### 11.13 失败与恢复

统一错误文案结构：发生了什么、影响什么、如何恢复。必须覆盖授权过期、位置不可读、舰船变化、货舱未知、预算不足、区域采集滞后、部分区域缺失、订单消失、范围不可达、安全约束失败、滑点转负、规划超时、无可行解和链条中断。保留最后可用计划但标记“不可按原值执行”，不得直接空白。

### 11.14 可观测性与验收

采集指标：区域覆盖率、活跃 Type 覆盖率、分页数/行数/字节、批次耗时、缓存命中、最老快照年龄、ESI error-limit。规划指标：候选数、过滤原因、求解耗时、Beam 截断、方案数、复核变化率。

验收必须包括：确定性和单调性属性测试、VWAP/买单范围/费用测试、批次原子性、重复订单、ESI 限流和 stale、账户隔离、路径安全硬约束、预算/货舱边界、组合最优性基准、链式不重复消费订单、性能预算、生产真实区域行情及失效复核。安全审计确认不暴露 Token、内部错误、主机信息和本地秘密。

### 11.15 分阶段交付

- P0 数据可信：全区域规范化订单、完整快照、覆盖状态、角色上下文、安全路线、单品计划和执行前复核。
- P1 组合装载：货篮、预算/货舱双约束、同站多品、边际贡献和排除原因。
- P2 链式调度：最多三段 Beam Search、时间线/泳道、每段资金与货舱、局部修复。
- P3 增强：装配后精确货舱、玩家建筑、击杀风险、历史表现与动态成交概率。

默认用户流程：授权只读角色 → 自动读取位置/舰船并确认货舱 → 输入采购预算与最低安全等级 → 快速返回最佳单品/组合/短链 → 查看解释与压力测试 → 加入计划冻结快照 → 执行前复核 → 使用逐站只读清单 → 记录预计与实际偏差。
