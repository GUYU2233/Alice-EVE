# Alice-EVE 数据层与缓存说明

更新时间：2026-09-19

## 1. 同步模型

- 自动同步任务按 `account_id + kind` 唯一，避免重新授权产生重复历史状态。
- 成功后依据 ESI `ExpiresAt` 重新进入 pending，形成周期刷新。
- 错误分类为授权、禁止、未找到、限流、上游、超时、网络、解码、超限。
- `GET /api/v1/eve/sync-status` 只返回当前 Alice 账户状态。
- 市场区域调度、采集任务、租约、重试和 last-good 快照持久化到 PostgreSQL。
- 区域市场分页采集采用有界并发和共享 ESI Error Limit 限流器；失败批次不可见。

## 2. 数据边界

- 自动快照只采集合理的个人只读数据。
- 不请求 write/manage/send/respond、舰队写、Waypoint/UI 操作及默认军团管理数据。
- Mail Body、Contract Items/Bids、Killmail Detail 使用摘要归属校验后的按需读取。
- Mail Body 不持久化，响应使用 `Cache-Control: no-store`。
- 公共 Killmail 和 Universe Names 请求不携带 Alice Bearer。
- 已被 CCP 废弃的 Chat Channels 不进入自动同步。
- Chatlogs/Gamelogs 原文默认只保存在本地，不自动上传。

## 3. SDE 与市场数据

- 服务端 SDE 采用版本化 build；只有完整导入并校验成功的 build 才能原子激活。
- 服务端保存物品、中文名称、体积、市场组、星系安全等级和星门图。
- 市场 current/previous 快照原子切换，失败批次不会污染 current。
- `region_market_order_depth_levels` 保存最多 32 档买卖深度，支持加权成交价和更完整的货舱利用。
- 桌面端使用独立 SDE SQLite；本地日志动态数据使用应用 SQLite，不与静态 SDE 混写。

## 4. 路线规划与缓存

服务端共享路线模块位于 `relay-server/internal/routeplanner`，不依赖市场业务，可供市场、合同、资产搬运和其他物流功能复用。

数据流：

```text
active SDE build ID
  → 不可变星系/星门邻接图
  → 有界 BFS 最短安全路线
  → 版本化路线结果缓存
  → 单条或批量 API
```

约束和语义：

- 最低安全等级与最大跳数为硬约束；最大跳数上限为 256。
- 同一星系内不同空间站是合法零星门跳。
- 不可达路线返回 `unavailable`，不得回退成零跳。
- 批量接口最多接受 500 条路线，并保持输入顺序。
- active SDE build 变化后加载完整新图并清空旧版本结果缓存。

缓存层级：

| 层级 | 内容 | 边界 |
|---|---|---|
| React | 单次规划的地点对去重 | 当前请求 |
| Desktop route cache | 路线结果 | 10 分钟，最多 2,048 条 |
| Desktop entity cache | 空间站、星系、物品类型 | 24 小时，最多 4,096 条 |
| Relay graph cache | 不可变星系/星门图 | active SDE build |
| Relay route cache | 可达和不可达路线 | SDE version + 完整约束；10 分钟，最多 4,096 条 |
| PostgreSQL/SQLite | 权威和耐久数据 | build、batch、事务 |
| localStorage | 最近一次市场规划 | 用户重新规划时覆盖 |

派生路线不写入磁盘。SDE build/version 是缓存正确性边界，而 TTL 主要用于内存回收。

## 5. 名称解析

- 单实体支持 Type、System、Corporation、Alliance、Station。
- 批量 `POST /api/v1/eve/public/universe/names` 支持 1–1000 个正整数 ID，去重排序后缓存。
- 桌面经济面板批量解析资产、交易、订单、蓝图、采矿物品名称；ID 仅作为最终回退。
- 路线时间线优先显示空间站名称，不再把 Station ID 作为主文案。

## 6. 验证状态

截至本次更新：

- Relay 全量 `go test ./... -count=1` 与 `go vet ./...` 通过。
- Desktop 全量 `go test ./... -count=1` 与 `go vet ./...` 通过。
- Vite production build 与 Wails production build 通过。
- 共享路线服务已部署并通过健康检查。
- Windows 环境未完成 Go race build：当前可用 C 编译器不能用于原生 Windows race binary；这不等同于 race test 已通过。

公开文档不记录真实生产域名、IP、主机别名、数据库连接、服务器绝对路径或账户标识。
