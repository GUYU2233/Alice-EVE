# Alice-EVE Releases v0.0.1-alpha

Latest artifacts only; older artifacts belong in `archive/`.

## Artifacts

- `eve-assistant-v0.0.1-alpha.exe` — Windows desktop build
- `eve-assistant-v0.0.1-alpha.zip` — Windows desktop package
- `eve-assistant-mobile-android-v0.0.1-alpha.apk` — Android mobile build
- `eve-assistant-mobile-windows.zip` — Windows mobile companion build

SHA-256 values for these files are recorded in [`SHA256SUMS.txt`](SHA256SUMS.txt). The binaries are intentionally ignored by Git; verify the files locally or in the release distribution before publishing.
