# EVE 助手当前工作记录

更新时间：2026-09-13

## 项目目标

构建一个本地优先的 EVE Online 中文信息助理：

- 桌面端：Go + Wails + React
- 手机端：Flutter
- 公网中继：Go Relay Server
- 本地数据：SQLite + 独立版本化 SDE SQLite
- 通信：HTTPS REST + SSE/WSS + FCM/APNs
- 功能边界：只读查询、情报聚合、风险分析和提醒；不读游戏内存、不注入、不自动操作游戏、不自动发信/下单/移动资产。

## 工作目录

```text
D:\Project\EVE助手
```

## 已完成的项目目录

### 1. 桌面端

```text
desktop-app/
├── go.mod
├── go.sum
├── main.go
├── wails.json
├── frontend_embed.go
├── README.md
├── internal/
│   ├── app/service.go
│   ├── protocol/events.go
│   └── storage/store.go
└── frontend/
    ├── package.json
    ├── package-lock.json
    ├── index.html
    ├── src/main.tsx
    ├── src/style.css
    └── dist/
```

当前桌面端使用 Go + Wails v2.15.0 + React/TypeScript/Vite。

已成功执行：

```bash
wails doctor
wails build -clean
```

构建产物：

```text
desktop-app/build/bin/eve-assistant.exe
```

React 前端也已成功执行：

```bash
npm install
npm run build
```

### 2. Relay Server

```text
relay-server/
├── go.mod
├── README.md
├── cmd/relay-server/main.go
└── internal/
    ├── api/server.go
    ├── api/server_test.go
    └── store/
        ├── memory.go
        └── store.go
```

当前 API：

```text
GET  /health
POST /api/v1/pair
POST /api/v1/pair/confirm
POST /api/v1/messages
GET  /api/v1/events
```

已实现：

- 六位随机配对码
- 配对码有效期 5 分钟
- 配对码一次性消费
- 设备 ID 和随机设备 Token 生成
- 设备 Token SHA-256 哈希保存
- 配对时保存设备类型、名称和公钥字段
- 消息 Envelope 校验
- 要求 `Authorization: Bearer ...`
- `messageId` 幂等去重
- 内存事件队列
- SSE 推送
- 健康检查

已成功执行：

```bash
cd relay-server
gofmt -w internal/api/server.go internal/api/server_test.go internal/store/memory.go internal/store/store.go
go test ./...
```

测试结果：

```text
ok relay-server/internal/api
?  relay-server/cmd/relay-server [no test files]
?  relay-server/internal/store [no test files]
```

注意：当前 Relay 仍是原型，设备 Token 目前只检查 Authorization Header 是否存在，还没有真正根据 Token hash 验证设备身份；存储仍是内存实现；SSE 事件队列还没有离线持久化。

### 3. 手机端

```text
mobile-app/
├── pubspec.yaml
├── README.md
└── lib/
    ├── main.dart
    ├── api/api_client.dart
    └── models/protocol.dart
```

当前包含：

- 登录占位页
- 设备配对占位页
- 告警列表占位页
- MessageEnvelope
- AlertEvent
- RelayApiClient 占位实现

已修正协议字段，使用：

```text
v
id
type
ts
sender
payload
```

Flutter SDK 已安装并可用，但 `mobile-app` 还是手写的最小项目目录，缺少 Flutter 标准平台工程目录。之前执行：

```bash
flutter build windows
```

失败原因：

```text
No Windows desktop project configured
```

下一步在 `mobile-app` 中执行：

```bash
flutter create .
flutter pub get
flutter analyze
flutter build windows
flutter build apk
```

执行前注意保留/合并现有 `lib/`、`pubspec.yaml` 和协议模型。

### 4. 共享协议

```text
shared/protocol/
├── envelope.schema.json
├── messages.schema.json
├── pairing.schema.json
├── errors.schema.json
├── openapi.yaml
└── README.md
```

协议说明：

```text
docs/protocol.md
```

协议已经定义：

- Versioned JSON Envelope
- 配对码流程
- 设备注册方向
- WSS/HTTPS 分工
- ACK 状态机
- Outbox、重试、离线同步
- 幂等
- 错误模型
- 设备权限
- FCM/APNs 边界
- Token 和隐私安全边界

### 5. 参考项目分析

```text
参考项目分析与架构建议.md
```

已分析并拉取的参考项目：

- eveai
- eve-mcp-server
- rift-intel-fusion-tool
- TheGuideToTheNewEden
- PySpy
- openclaw-eve-skill
- localisprimary-esi
- goesi-upstream
- aladac-sde
- Marquette
- eve-market-browser
- eveeyeevemaps

参考项目代码位于：

```text
参考项目/
```

## 服务器信息

服务器别名：`sedcn2`

```text
公网 IP：156.239.242.141
Cloudflare Zone：guyufl.us.ci
目标域名：alice.guyufl.us.ci
Zone ID：4bc19d590a7b94ef133866755f7759d9
```

服务器运维记录：

```text
/root/cf/server-network-architecture.md
```

实际运维文档中说明真实文件已迁移至：

```text
/root/ops/server-network-architecture.md
```

并在 `/root/cf/` 保留符号链接。

现有入口：

```text
Cloudflare CDN
    ↓
Xray :443
    ↓
Nginx :8443
    ↓
本机服务
```

当前已有：

- `dsh.guyufl.us.ci`
- `www.guyufl.us.ci`
- `opencode.guyufl.us.ci`

`alice.guyufl.us.ci` 尚未完成 DNS/Nginx/Relay 部署。

已读取服务器 Cloudflare 运维 Skill：

```text
/root/.dsh/skills/cloudflare/SKILL.md
```

Cloudflare 操作应优先使用：

```bash
/root/.local/bin/cf
```

不要输出或询问用户粘贴 API Token。删除 DNS、修改 SSL、防火墙等操作前必须确认。

## 推荐公网部署方案

```text
alice.guyufl.us.ci
        ↓
Cloudflare 橙云
        ↓
Xray :443
        ↓
Nginx :8443
        ↓
Go Relay 127.0.0.1:18443
```

建议：

- Relay 只监听 `127.0.0.1:18443`
- Nginx 新增 `alice.guyufl.us.ci` vhost
- Cloudflare 新增橙云 A 记录
- 公网服务通过 Nginx/Cloudflare 提供 HTTPS
- Relay 自身仍需设备认证，不能只依赖 Cloudflare
- 服务器不保存桌面端 EVE Token

本次尚未修改服务器配置，也尚未新增 DNS 记录或 Nginx 配置。

## 当前通信闭环

原型已经形成：

```text
桌面端请求配对码
        ↓
Relay 生成六位临时配对码
        ↓
手机提交配对码
        ↓
Relay 一次性消费
        ↓
生成 deviceId + deviceToken
        ↓
桌面端提交告警消息
        ↓
Relay 做 Envelope 校验和 messageId 幂等
        ↓
SSE 推送给客户端
```

## 后续工作进展（2026-09-13）

- Wails Windows 发布构建成功：`desktop-app/build/bin/eve-assistant.exe`。
- Desktop Outbox HTTP 发送、ACK 匹配和指数退避已实现并通过 Go 测试。
- Relay ACK 接口、PKCE、Intel 风险模型已实现并通过测试。
- Flutter ESI Gateway 已实现 User-Agent、超时、ETag 缓存和错误分类。
- PostgreSQL 备份 timer、healthcheck、logrotate 已部署。
- Flutter analyze/test 通过；已移除未使用的 firebase_messaging 依赖。Windows 发布构建已成功，产物位于 `mobile-app/build/eve-assistant-mobile-windows.zip`，并生成 SHA256 与发布说明；APK 尚未构建。

## P0-P2 迁移进展（2026-09-13）

- Relay 已加入设备 Token SHA-256 校验、撤销接口和 1 MiB 请求限制；测试通过。
- 桌面端已加入 SQLite Outbox、ACK、重试策略和同步游标；Go 测试通过。
- mobile-app 已生成 Flutter 标准平台工程，`flutter pub get`、`flutter analyze` 通过。
- Flutter Windows 构建受 Firebase 依赖下载网络超时影响；真实事件列表/SSE 仍待完善。
- Relay 已完成 PostgreSQL Store 注入、迁移和 systemd 部署；设备/配对/消息基础数据写入 PostgreSQL。
- alice.guyufl.us.ci 已配置 Cloudflare 橙云与 Nginx 反代，公网健康、配对、消息、告警和撤销 smoke test 已通过。
- PostgreSQL 备份 timer、healthcheck、logrotate 已部署。

## 下一步优先级

### P0：先修安全和持久化

1. 真正验证设备 Token hash
2. 设备 Token 撤销接口
3. 设备存储 SQLite/PostgreSQL
4. 配对码持久化和过期清理
5. 消息大小限制、请求限流和审计
6. CORS/Origin/HTTPS/WSS 安全配置
7. 设备公钥签名或至少绑定设备密钥

### P1：完成可靠消息

1. 桌面端本地 Outbox
2. Relay ACK
3. 离线消息
4. 重试和指数退避
5. 手机端已读确认
6. 断线重连和同步游标

### P2：真实三端连接

1. Wails 正式绑定 React 页面
2. Flutter 执行 `flutter create .`
3. Flutter 调用真实配对接口
4. 桌面端发布测试告警
5. 手机端接收告警
6. Android FCM
7. iOS APNs

### P3：EVE 能力

1. SDE SQLite 导入
2. ESI Gateway
3. EVE SSO/PKCE
4. zKillboard
5. Local/聊天日志导入
6. Intel 解析和上下文合并
7. 路线/威胁引擎
8. 市场查询

## v0.0.1-alpha 发布状态与说明模板（2026-09-13）

> 当前工作区未发现 Git 仓库元数据（`git status` 在项目根目录返回 not a git repository），因此本节仅记录发布判断，不代表已创建 tag 或上传 Release。

### 发布结论

建议以 **v0.0.1-alpha（内部/技术预览）** 发布，范围限定为 Windows 桌面端 + Go Relay MVP；不宣称三端生产可用。已记录的最新进展显示公网 Relay smoke test、PostgreSQL、ACK/重试和令牌校验已完成，但仍需在正式发布前以实际构建与部署环境复核。

### 已实现（按当前记录）

- Windows Wails 桌面端可构建：`desktop-app/build/bin/eve-assistant.exe`。
- Desktop SQLite Outbox、ACK 匹配、指数退避、同步游标；Go 测试通过。
- Relay：设备 Token SHA-256 校验、撤销接口、1 MiB 请求限制、ACK、PostgreSQL Store/迁移及 systemd 部署。
- Relay 公网域名 `alice.guyufl.us.ci` 的 Cloudflare/Nginx 反代与健康、配对、消息、告警、撤销 smoke test 已记录通过。
- Flutter ESI Gateway（User-Agent、超时、ETag、错误分类）；`flutter analyze/test` 通过。
- PostgreSQL 备份 timer、healthcheck、logrotate 已部署。

### 未实现/不承诺

- 移动端真实事件列表/SSE、完整三端联调；Android FCM 与 iOS APNs。
- APK 发布包（当前仅记录 Windows mobile zip，APK 尚未构建）。
- 完整 Wails React 业务绑定、完整 EVE 能力（SDE、SSO/PKCE 全流程、zKillboard、日志导入、市场/地图）。
- 生产级设备密钥签名、完整限流/CORS/Origin 策略及离线消息全链路（需再次验收）。
- 不支持读游戏内存、注入、自动操作、自动发信/下单/移动资产。

### Relay 配置与运行要求

- 开发：`cd relay-server && go test ./... && go run ./cmd/relay-server`。
- 运行需 Go、可访问的 PostgreSQL（生产通过迁移初始化）、反向代理 TLS；Relay 不应直接裸暴露公网。
- 配置应提供监听地址、PostgreSQL DSN、公开基础 URL、设备令牌密钥/哈希策略及日志级别；敏感值使用环境变量或 Secret，不提交仓库。
- 客户端只保存 EVE SSO token 于桌面端安全存储；Relay/手机/模型不得接收 EVE token。
- 发布前必须复核 `/health`、配对一次性/过期、授权消息、撤销后拒绝、ACK/重试、1 MiB 限制、TLS 与数据库备份恢复。

### Release Notes 模板

```markdown
# v0.0.1-alpha — 技术预览

本版本用于内部测试，不建议生产环境使用。

## Included
- Windows desktop build and Relay MVP
- Device token auth/revocation, PostgreSQL persistence, ACK/retry
- ESI gateway caching/error handling

## Known limitations
- Mobile realtime delivery, FCM/APNs and APK are not included
- EVE SSO/SSO-dependent features and gameplay automation are not supported
- Deployment/security settings require operator verification

## Requirements
- Windows desktop: Wails artifact and network access to Relay
- Relay: Go, PostgreSQL, TLS reverse proxy, environment-based secrets
- Never upload EVE SSO tokens to Relay or mobile clients
```

## 注意事项

- 当前 Relay `POST /api/v1/messages` 虽要求 Authorization Header，但还未根据设备数据库验证 Token。
- 当前配对返回的设备 Token 只保存 SHA-256，但内存服务重启后所有设备都会丢失。
- 当前设备 Token 使用随机字符串返回一次，生产环境必须加入撤销、过期和轮换。
- 当前 Wails 已能构建 `eve-assistant.exe`，但业务功能仍是骨架。
- 当前 Flutter 工具链可用，但项目缺少标准平台目录。
- 当前 Relay 生产部署没有执行，不应直接暴露公网。
- `alice.guyufl.us.ci` 的 DNS/Nginx 尚未配置。
- 所有 ESI 请求未来必须有 User-Agent、ETag、Cache-Control、Retry-After、429/420 和超时处理。
- 所有角色私有数据必须按角色和用户隔离。
- 不要把 EVE Token 上传到 Relay、手机或模型。

## 迁移后恢复工作建议

进入工作区后，先读取本文件，然后检查：

```text
参考项目分析与架构建议.md
当前工作记录.md
shared/protocol/
docs/protocol.md
desktop-app/
relay-server/
mobile-app/
```

然后优先执行：

```bash
cd desktop-app
wails doctor
wails build -clean

cd ../relay-server
gofmt -w .
go test ./...

cd ../mobile-app
flutter create .
flutter pub get
flutter analyze
flutter build windows
flutter build apk
```

公网部署前必须再次读取服务器：

```text
/root/cf/server-network-architecture.md
/root/.dsh/skills/cloudflare/SKILL.md
```
