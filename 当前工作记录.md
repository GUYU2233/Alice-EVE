# Alice-EVE 当前工作记录

更新时间：2026-09-19（市场规划、共享路线与缓存阶段）

> 本文件是当前执行状态和下一步计划的唯一工作入口。架构依据见 `参考项目分析与本系统架构.md` 第 10 节。旧记录中与本文件冲突的 Node/Fastify、SSE-only、服务端绝不保存 EVE Token、手工文本为主流程等结论均已废止。

## 1. 当前产品目标

构建本地优先、服务端可持续同步的 EVE Online 中文信息助理：

- 桌面端：Go + Wails + React/TypeScript；
- 手机端：Flutter；
- 公网服务端：Go + PostgreSQL；
- 本地动态数据：SQLite；
- 静态 EVE 数据：独立版本化 SDE SQLite；
- 实时通信：HTTPS REST + WSS；
- 移动推送：FCM/APNs（尚未完成生产接入）；
- 功能：本地日志、角色/ESI、市场、资产/订单、击杀/通知、Intel、风险、告警、Agent；
- 红线：不读游戏内存、不注入、不模拟操作、不自动驾驶、不自动下单/移动资产/发邮件。

核心数据面：

```text
Chatlogs + Gamelogs（本机实时）
EVE SSO + ESI（角色私有）
Public ESI + Market（共享公共）
SDE（静态知识）
          ↓
统一事件/实体/快照/新鲜度
          ↓
SQLite + PostgreSQL + SDE SQLite
          ↓
情报、角色、市场、告警、Agent、手机
```

## 2. 安全配置与 `EveClient.json`

`EveClient.json` 是本机忽略文件，来自 EVE Developer Portal，包含 Client ID、Client Secret、Callback URL 和应用可申请 scopes。它的作用是定义 EVE SSO/ESI 应用能力，不是业务数据文件。

约束：

- 文件保持 `.gitignore`；不得提交、输出或复制到桌面/前端/移动 bundle；
- Client Secret 只在 Alice 服务端以 Secret/环境变量使用；
- 当前 production 已私有配置 confidential-client credential；
- scopes 是“可请求全集”，不代表当前角色已经授权或系统已经同步；
- 后续按 profile 渐进授权，不默认一次请求全部权限；
- 文档不记录任何真实 Client ID、Secret、Token、域名或生产 DSN。

## 3. 当前真实完成状态

### 3.0 当前里程碑（2026-09-19）

本节覆盖下面仍保留的旧阶段描述；旧描述仅用于追溯。

已完成并验证：

- Chatlogs/Gamelogs 已迁入统一 `internal/ingest` Tailer，完整记录成功持久化后才推进 committed cursor；
- SQLite 已落地 `log_sources/log_files/log_cursors/normalized_local_events`，事件与游标在同一事务提交；
- UTF-8/UTF-16LE、首次 EOF baseline、半行跨重启、handler 失败、截断和稳定事件 ID 均有测试；
- 聊天 Intel 和战斗事件已接入桌面 UI；
- 正式 `internal/evesde` SQLite 已支持类型、星系、地图、市场组、蓝图/材料，并通过临时库构建、校验和原子切换；
- 旧 `eve.SDEIndex` 与正式 JSONL/SQLite 后端已有兼容桥；
- 已实现加密 EVE refresh grant、同步任务、角色快照和公共缓存相关迁移；
- refresh grant 使用认证加密，access token 仅短时驻留内存，客户端和模型不接触 EVE Token；
- ESI Gateway 已支持 User-Agent、ETag、Expires、分页、Retry-After、error-limit 与响应大小限制；
- 角色同步域覆盖身份、位置、舰船、技能、经济、资产、订单、击杀和通知等只读数据；
- 公共 ESI 支持状态、价格、Universe、区域订单和历史，并写入 PostgreSQL cache；
- 服务端提供账户隔离的角色快照 API；桌面已有 typed Client 和 Wails bridge；
- 服务端、桌面 Go tests/vet、Vite/Wails 构建均已通过；
- 区域市场持久化调度、原子快照、32 档订单深度、全品类候选、同路线组合与取送链式规划已完成；
- 共享路线模块、单条/批量路线 API、服务端版本化图缓存和桌面应用级路线/实体缓存已完成；
- Command Console 风格的角色、经济、市场、Intel、告警和 Agent 工作区已落地；
- 最近一次市场规划可跨页面恢复，路线时间线优先显示空间站名称。

待完成：

- 多版本规划记录的命名、删除与对比；
- 玩家建筑名称和权限受限位置的完整解析；
- Windows Known Folder 与更完整的 SDE 官方下载/更新硬化；
- FCM/APNs、完整三端实时联调、正式代码签名和自动更新发布链。


### 3.1 Git 与构建

- 仓库：GitHub `main`；
- 仓库主分支：`main`；
- Windows Wails 构建可用：`desktop-app/build/bin/eve-assistant.exe`；
- Relay/Desktop 全量 Go tests/vet、Vite build 和 Wails build 已通过；
- 构建产物、环境文件、数据库、本地工具状态和生产运维标识不得提交。

### 3.2 服务端

已实现并部署：

- PostgreSQL 持久化和迁移；
- Alice account/device/session；
- 设备 Token 校验/撤销；
- WSS、游标恢复、durable events/ACK；
- transactional outbox；
- PostgreSQL LISTEN/NOTIFY 作为 wakeup，数据库回放为权威；
- Conversation/Message、Agent Run/Event/Cancel API 基础层；
- EVE SSO authorization code + PKCE；
- confidential-client HTTP Basic token exchange；
- 浏览器 callback 暂存 code、桌面轮询领取；
- OAuth 登录已通过真实 EVE 测试。

当前 EVE OAuth 只用于身份验证：EVE access token 用于身份查询后即丢弃；尚未保存 EVE refresh token，不能持续同步 ESI。

### 3.3 桌面 OAuth 与安全存储

已完成：

- React 不接触 PKCE verifier；
- HTTP 202 被正确识别为 pending 并继续轮询；
- 成功前不清除 state/verifier；
- Windows DPAPI 参数修复；
- DPAPI save/load round-trip 测试通过；
- 登录后 UI 显示服务器已连接、设备已授权/绑定。

### 3.4 本地 Chatlogs 采集（开发中）

已实现：

- 默认探测 `Documents/EVE/logs/Chatlogs` 和 OneDrive 变体；
- 用户可覆盖目录；
- `.txt` 扫描、增量 offset、SQLite `chat_log_cursor`；
- UTF-16LE/UTF-8、半行、截断；
- Header 中 Channel Name / Listener；
- 时间、作者、消息解析；
- 稳定事件 ID 和内存去重；
- 首次从 EOF 开始，避免历史洪泛；
- 原文不上传；结构化 finding 可显式发布告警；
- 真实 EVE Chatlogs 文件已验证，offset == size。

不足：

- 目录发现尚未使用 Windows Known Folder API；
- watcher 当前为 1 秒扫描，不是事件通知 + overflow 重扫；
- 多角色/频道活跃文件选择和持久去重需完善；
- parser 仍使用少量硬编码星系/舰船，必须由 SDE 替代；
- UI/数据源健康状态尚未完成最终验证。

### 3.5 本地 Gamelogs 采集（开发中）

用户真实战斗证明战斗数据写入 `EVE/logs/Gamelogs`，不是 Chatlogs。真实文件已确认包含 `combat`、`notify`、`bounty` 等事件。

当前未提交代码已加入：

- Gamelogs 自动发现与增量读取；
- 共享 SQLite cursor；
- combat/notify 基础解析；
- under_attack、attacking、warp_scrambled、generic combat；
- 清理日志 HTML/颜色标记；
- 噪音过滤和测试。

测试已通过，但尚未接到前端、尚未重新构建最终 EXE、尚未完成真实新增战斗事件端到端验收。

### 3.6 前端现状

已加入但尚未最终验收：

- 实时 Chatlogs 状态卡；
- 最近聊天结构化 Intel；
- 显式发布告警按钮；
- 手工输入降级为折叠的备用/调试入口；
- 设置中 Chatlogs 路径。

Agent 页面为空的原因：数据/API 基础存在，但 OAuth 后尚未幂等创建默认 Conversation，也未完成完整 Agent 执行闭环。当前阶段不优先修饰 Agent 空壳，先完成数据底座。

### 3.7 手机端

Flutter 工程和协议基础存在；生产级移动实时事件、FCM/APNs 和完整三端联调尚未完成。手机端不得持有 EVE Token。

## 4. 参考项目结论

### RIFT

应完整借鉴而不是只借鉴 Chatlogs：

- `ChatLogsObserver` / `ChatLogFileParser`；
- `GameLogsObserver` / `GameLogWatcher` / `GameLogMessageParser`；
- Windows/macOS/Linux 日志目录探测；
- offset、轮转、overflow 重扫、最新活跃日志；
- Intel 状态合并和风险/告警；
- ESI、SDE、市场、击杀数据的组合。

### eveai

借鉴 ESI/Feed 游标、首次建立 head baseline、提交后推进 cursor、失败不跳事件、工具和多渠道架构；它不提供 EVE 本地 Chatlogs/Gamelogs watcher。

### ESI/MCP/Market 参考

- `eve-mcp-server` / `goesi-upstream` / `localisprimary-esi`：ESI 分域、OpenAPI、Context、缓存、限流；
- `eve-market-browser` / `Marquette`：市场采集、规范化、快照和查询分层；
- `aladac-sde`：SDE 类型化、版本化和更新流水线；
- `TheGuideToTheNewEden` / PySpy：中文用户场景和情报 UX。

## 5. 当前架构决策

### 5.1 ESI Token

目标方案：Alice 服务端加密保存每角色 EVE refresh token，使桌面关闭后仍能同步并支持手机共享数据。

实施前必须具备：

- envelope encryption 和 key version；
- grant/scopes/revocation/schema；
- access token 只短时驻留；
- refresh single-flight；
- 数据删除和 scope upgrade；
- 客户端/模型永不获得 EVE Token。

### 5.2 Scope Profiles

不使用 `EveClient.json` 的全部 scopes 作为默认授权。规划：

1. `identity`：身份、在线、位置、舰船；
2. `character`：技能、队列、克隆、植入体；
3. `economy`：钱包、资产、个人订单、合同；
4. `combat`：击杀、通知、联系人/standings；
5. `industry`；
6. `social`；
7. `corporation`。

首批目标为只读 `identity + character + economy + combat`；写入型 scopes 默认关闭。

### 5.3 隐私

- Chatlogs/Gamelogs 原文不自动上传；
- 默认只在桌面本地保存结构化事件；
- 用户可指定 Intel 频道和自动发布规则；
- 高风险战斗事件可选择本地通知或发布 Alice 告警；
- 钱包、资产、邮件等敏感数据按角色隔离，推送只包含摘要。

## 6. 数据模型与存储计划

### 桌面 SQLite

```text
log_sources
log_files
log_cursors
normalized_local_events
local_intel
local_alert_rules
outbox
sync_cursor
pairing_state
ui_preferences
```

当前只有 `chat_log_cursor` 原型，需迁移为通用表，不能让 Gamelogs 长期借用 chat 命名。

### 服务端 PostgreSQL

```text
accounts / characters
devices / sessions
encrypted_character_oauth_grants
character_scope_snapshots
esi_sync_jobs / esi_sync_cursors
character_snapshots
wallets / assets / orders / contracts
killmails / notifications
public_cache / market_snapshots
alerts / deliveries
conversations / runs / run_events
```

### 独立 SDE SQLite

```text
types / groups / categories
systems / constellations / regions / stargates
market_groups
blueprints / activities / materials
```

## 7. 实施计划和验收

### Phase 0 — 文档与架构冻结（当前）

- [x] 纠正“手工输入/Chatlogs 即产品”的范围错误；
- [x] 明确本地、ESI、公共市场、SDE 四层数据面；
- [x] 明确 `EveClient.json` 用途；
- [x] 明确服务端加密 refresh token 的目标边界；
- [ ] 审阅本计划后再恢复实现。

### Phase 1 — 统一本地采集层

- [ ] 将 Chat/Game watcher 合并为通用可靠 tail framework；
- [ ] 通用 `log_sources/log_files/log_cursors/normalized_local_events` migrations/repositories；
- [ ] Windows Known Documents + OneDrive + 手动配置；
- [ ] UTF-8/UTF-16LE、共享读、半行、截断、轮转、重启游标；
- [ ] Chat metadata、多角色/频道、Game Session metadata；
- [ ] combat/notify/mining/bounty/session 类型化；
- [ ] 频道白名单、事件规则、隐私设置；
- [ ] 数据源健康 UI、最近事件 UI；
- [ ] 真实 EVE Chat + Combat 端到端验收；
- [ ] 重启不重复、历史不洪泛、原文不上传的集成测试。

### Phase 2 — 正式 SDE

- [ ] 选择 CCP 官方 SDE 获取/更新策略；
- [ ] 构建版本化只读 SQLite；
- [ ] type/system/ship/market group/blueprint/map repositories；
- [ ] checksum、atomic switch、rollback；
- [ ] 用 SDE resolver 取代硬编码星系/舰船。

### Phase 3 — 持续 ESI Grant 与 Gateway

- [ ] PostgreSQL migration：characters/grants/scopes/jobs/cursors；
- [ ] refresh token encryption/key rotation；
- [ ] scope profiles 和增量重新授权；
- [ ] ESI transport：User-Agent、ETag、Expires、分页、error limit、退避；
- [ ] identity/online/location/ship；
- [ ] skills/queue/clones/implants；
- [ ] 健康状态、stale/freshness、撤销和删除。

### Phase 4 — 经济/战斗私有数据

- [ ] wallet/assets/orders/contracts；
- [ ] killmails/notifications/contacts/standings；
- [ ] 角色中心 API/UI；
- [ ] 多角色隔离和权限错误验收。

### Phase 5 — 公共 Universe 和市场

- [ ] 公共 Universe/Route 数据；
- [ ] region orders、history、prices；
- [ ] Jita/主要交易中心按需查询；
- [ ] shared cache/request coalescing；
- [ ] 估价和价格告警；
- [ ] 后续热门 region/type 增量快照，不做全宇宙高频扫。

### Phase 6 — Intel/风险/告警

- [ ] 统一 local/ESI/kill/market events；
- [ ] Resolver/Context Merger/State Store/Rule Engine；
- [ ] 结构化 evidence、confidence、TTL；
- [ ] WSS/outbox/ACK 端到端；
- [ ] 手机告警和用户可配置自动发布。

### Phase 7 — 高级域和 Agent

- [ ] industry/mining/PI；
- [ ] mail/calendar/fleet/corporation（按需授权）；
- [ ] 默认 Conversation 创建；
- [ ] Agent 工具权限、只读执行、引用和审计。

## 8. 恢复工作时的固定顺序

```text
1. 读取本文件
2. 读取《参考项目分析与本系统架构.md》第 10 节
3. 检查 git status，保护用户无关改动和本地 Secret
4. 完成 Phase 1，不再零散追加单一 watcher/UI
5. 完成正式 SDE
6. 实现 ESI grant + Gateway
7. 再扩角色、市场、Intel 和 Agent UI
```

测试命令：

```powershell
cd desktop-app
gofmt -w <target files>
go test ./... -count=1
go vet ./...

cd frontend
npm run build

cd ..
wails build -clean

cd ../relay-server
go test ./... -count=1
go vet ./...

cd ../mobile-app
flutter analyze
flutter test
```

提交前：

- 运行 `scripts/security-audit.ps1` 并 fail-fast；
- 仅 stage 目标文件；
- 不提交 `EveClient.json`、`.env.local`、生产环境文件、二进制；
- 不恢复或覆盖用户无关的删除/未跟踪文件；
- GitHub 保持公开脱敏，本机开发配置保留真实值。

## 8.1 完整只读角色数据实施状态（2026-09-16 夜间）

已完成代码、测试与生产部署：

- 角色同步域扩展为 34 个：身份、在线、位置、舰船、技能/队列、钱包/流水/交易、资产、当前/历史订单、击杀、通知、克隆、植入体、疲劳、忠诚点、声望、头衔、蓝图、工业、采矿、合同、日历、联系人/标签、邮件标签/标题、聊天频道、研究、阵营战争、勋章、Fittings；
- 所有新增域仅使用个人只读 scope；未加入 write/manage/send/respond/waypoint/fleet-write 及默认军团数据权限；
- 成功同步任务会依据 ESI ExpiresAt 重新进入 pending，修复一次性快照问题；
- 公共名称解析新增 Corporation、Alliance、NPC Station，并继续支持 System/Type；
- 桌面新增经济多标签和活动页面，明确区分未授权、未同步、空数据与数值 0；
- 已完成扩展只读 scope 授权和多域快照验证；
- 资产、蓝图、联系人、植入体、工业、击杀、忠诚点、采矿、通知、订单、技能与钱包等域均有数据模型和测试覆盖；
- `contracts`/`orders` 的 ESI 空值会统一把 `null` 规范化为 `[]`，避免 JSONB 存储失败；
- 已移除废弃数据域并为上游错误提供退避重试。

## 8.2 数据层第二阶段完善（2026-09-16）

- 修正 Mail Labels DTO：ESI 返回对象 `{labels,total_unread_count}` 而非数组，生产已获取 Inbox/Sent/Corp/Alliance 标签快照；
- 根据 CCP 官方废弃说明移除 `chat_channels` 自动同步任务，避免永久无效重试；
- 同步错误细分为 unauthorized、forbidden、not_found、rate_limited、upstream、timeout、network、decode、too_large；
- 新增账户隔离 `GET /api/v1/eve/sync-status`，桌面设置页显示数据域、状态、下次运行与尝试次数；
- migration 020 将同步任务约束改为每个 `account_id + kind` 唯一，并支持清理重复历史任务；
- 新增安全批量名称接口 `POST /api/v1/eve/public/universe/names`，1–1000 个 ID、去重排序、公共缓存、无 Bearer；
- 经济面板已批量解析资产、钱包交易、当前/历史订单、蓝图和采矿 Type 名称；
- 新增按需合同 items/bids、邮件正文和 killmail detail；均先校验当前账户角色摘要归属，邮件正文 `no-store` 且不持久化，公共 killmail 不带 Bearer；
- 同步任务和角色快照域已完成持续刷新验证。

## 8.3 Command Console 数据呈现完善（2026-09-17）

- 经济面板使用本地 SDE 名称优先、公共 ESI 名称回退、Type ID 最终回退；中文名称为主文本，Type ID 为灰色次要信息；
- 正式桌面启动时从应用数据目录读取 SDE 并重建本地索引；中文实体解析已验证；
- 设置页展示同步任务，支持状态筛选、状态徽标、下次运行、新鲜度、尝试次数和服务端错误原因；
- 合同列表支持显式查看 Items/Bids，合同物品接入本地 SDE 名称；
- 击杀列表支持详情抽屉，结构化展示受害者、参与方、舰船/武器、掉落与损毁物品，并解析本地名称；
- 邮件标签支持显式查看正文，正文按需读取、不持久化、显示 no-store 隐私提示，并限制允许的 HTML 标签；
- 本地授权恢复错误不再静默吞掉，启动时会通过 UI 错误栏显示；
- 同步任务与唯一 `account_id + kind` 约束一致。

## 9. 当前立即下一步

1. 增加多版本规划记录：命名、恢复、删除和约束/收益对比；
2. 测量首次路线批量查询与桌面/服务端缓存命中的延迟，建立可观测指标；
3. 完善玩家建筑和权限受限地点名称解析，不伪造未知名称；
4. 为桌面本地 SDE 路线实现复用共享引擎或等价不可变图快照，消除数据库切换竞态；
5. 增加 Linux race detector/并发压力验证；
6. 完成移动端实时事件、FCM/APNs、正式签名和更新发布链。

设计原则：采用紧凑桌面数据库客户端的信息结构，不复制参考产品品牌；Alice-EVE 应像长期运行的数据工具，而不是 AI 营销仪表板。
## 2026-09-18：全区域贸易调度系统

- 已将完整实施架构写入《参考项目分析与本系统架构.md》第 11 节，贸易系统由五中心价差查询升级为全区域、有状态的运输计划工作台。
- Relay 新增 Migration 021 与 `internal/marketdata`：区域逐页采集、规范化订单、不可见暂存批次、完整覆盖后原子发布、current/previous 快照和 last-good 保留。
- ESI Gateway 新增有界单页 Region Orders；采集端每页/每批有硬上限，页数变化或中途失败不发布半快照。
- 新增认证区域采集、组合装载和链式规划 API；高成本计划接口不再允许匿名调用。
- `internal/trade` 已修复共同成交量未重算 VWAP 和忽略 MinVolume 的正确性问题；实现订单范围校验、预算/货舱双约束组合、最多四停靠/三航段 Beam Search，评分与置信度分离。
- Desktop 新增角色贸易上下文：自动读取 location/ship/wallet，区分钱包和本趟预算，保留现金，货舱明确 exact/estimated/source。
- SDE 新增 shortest/safest 路线和最低安全等级硬约束，返回完整星系、跳数、路径最低安全等级及低安/零安计数。
- Global Logistics Planner 桌面页已接入角色、位置、舰船、预算、现金预留、基础货舱估算和最低安全等级；组合/链式 Planner 已具备 RelayClient 与 Wails Bridge。
- Migration 021、健康检查、未认证规划 401、五中心报价和单品计划均完成 E2E 验证。
- 后续已完成持久化区域调度、服务端完整 SDE、订单深度和共享路线缓存；玩家建筑、装配后精确货舱和实时击杀风险仍属于后续增强边界。

## 2026-09-19：共享路线与分层缓存

- 新增 `relay-server/internal/routeplanner`，使用 active SDE build 的不可变星门图和有界 BFS；旧 PostgreSQL 递归路线实现已删除。
- `GET/POST /api/v1/trade/routes` 分别提供单条和最多 500 条批量路线；不可达项返回 `unavailable`，不再伪装为零跳。
- 服务端路线缓存键包含 SDE version、起终点、安全等级浮点位和最大跳数；10 分钟 TTL、最多 4,096 条，build 切换时失效。
- Desktop `RelayClient` 新增批量路线和 10 分钟/2,048 条应用级缓存；市场页一次规划只进行一次批量 Bridge/HTTP 调用。
- Desktop 新增 24 小时/4,096 条实体缓存，覆盖 Station/System/Type；仅缓存成功响应。
- 最近一次市场规划保存到本地 `alice.market.plan.v2`，跨页面返回时恢复方案和选择状态。
- Relay/Desktop 全量 tests/vet、Vite/Wails build 已通过；服务端部署和健康检查流程已验证。
